pipeline {
    agent any 
    stages {
        stage('install dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('testing code') {
            steps {
                sh 'npm test'
            }
        }
    }
}