pipeline {
    agent any 
    stages {
        stage('install dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('testing code') {
            steos {
                sh 'npm test'
            }
        }
    }
}